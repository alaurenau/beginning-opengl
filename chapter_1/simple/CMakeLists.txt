CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(simple)

SET(SOURCE_FILES
	src/main.cpp
)

ADD_EXECUTABLE(simple ${SOURCE_FILES})

IF(WIN32)
	SET(LIBRARIES OPENGL32 GLU32)
ELSE(WIN32)
	SET(LIBRARIES GL GLU)
ENDIF(WIN32)

TARGET_LINK_LIBRARIES(simple ${LIBRARIES} glfw)
